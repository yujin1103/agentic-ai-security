# 이 하니스는 mock 백엔드만 쓸 경우 표준 라이브러리만으로 동작한다(추가 설치 불필요).
#
# 실모델(ollama) 백엔드는 외부 패키지 없이 urllib(stdlib)로 호출하므로 역시 추가 설치가 없다.
# 단, 로컬에 Ollama 데몬과 모델이 설치되어 있어야 한다:
#   1) https://ollama.com 에서 Ollama 설치
#   2) 모델 받기:  ollama pull llama3.1   (또는 qwen2.5, mistral 등)
#   3) 서버 실행:  ollama serve   (보통 설치 시 자동 실행됨, http://localhost:11434)
#
# (선택) 결과 분석/시각화를 직접 할 경우에만 아래를 설치:
# pandas>=2.0
# matplotlib>=3.7
